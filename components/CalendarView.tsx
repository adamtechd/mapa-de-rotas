
import React from 'react';

const CalendarView: React.FC = () => {
  return (
    <div style={{ padding: '1rem', textAlign: 'center', border: '1px dashed #ccc', margin: '1rem', color: '#666' }}>
      <p>Esta visualização de calendário não está em uso e foi desativada para resolver erros de compilação.</p>
    </div>
  );
};

export default CalendarView;
